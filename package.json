{
  "name": "mtls-playwright-demo",
  "version": "1.0.0",
  "scripts": {
    "start:api": "node api-server/server.js",
    "start:web": "npx http-server web-app -p 8070",
    "test": "npx playwright test -c playwright.config.ts --workers=1"
  },
  "devDependencies": {
    "@playwright/test": "^1.30.0",
    "http-server": "^14.1.1"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
